{
  "private": true,
  "name": "@vladnets/ecs",
  "version": "1.0.0-alpha.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:once": "webpack",
    "build:watch": "webpack --watch --display minimal",
    "exec:once": "node dist/index.js",
    "exec:watch": "nodemon -q --watch dist -e js dist/index.js",
    "build-exec:once": "npm run build:once && npm run exec:once",
    "build-exec:watch": "npm run build: once && concurrently 'npm:build:watch' 'npm:exec:watch'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^12.0.2",
    "concurrently": "^4.1.0",
    "nodemon": "^1.19.0",
    "ts-loader": "^6.0.1",
    "typescript": "^3.4.5",
    "webpack": "^4.32.2",
    "webpack-cli": "^3.3.2"
  }
}
